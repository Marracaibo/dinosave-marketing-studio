# Video Remix Studio - Backend Environment Variables

# Server settings
HOST=0.0.0.0
PORT=8000
DEBUG=true

# Storage paths (default: relative to backend folder)
UPLOAD_DIR=uploads
TEMP_DIR=temp
OUTPUT_DIR=output
ASSETS_DIR=assets

# Optional: Custom FFmpeg path (if not in system PATH)
# FFMPEG_PATH=/usr/local/bin/ffmpeg
